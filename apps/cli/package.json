{
  "name": "@tamandua/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "tamandua": "dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@tamandua/core": "workspace:*",
    "@tamandua/persistence": "workspace:*",
    "@tamandua/service": "workspace:*"
  }
}
