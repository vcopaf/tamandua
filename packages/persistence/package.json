{
  "name": "@tamandua/persistence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tamandua/core": "workspace:*",
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.38.3"
  }
}
